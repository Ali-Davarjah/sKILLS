# اسکیل‌های پگاه — نقشه‌ی خانواده

هشت اسکیل روی یک دیتابیس. این صفحه می‌گوید **کدام سؤال به کدام اسکیل
می‌رود** و **اسکیماها چطور به هم وصل‌اند**. عددها از `skills-index.json`
می‌آیند که `crosslink.py` می‌سازد — دستی به‌روزشان نکن.

## سؤال کجا می‌رود

| سؤال | اسکیل | جنس |
|---|---|---|
| نمره و رتبه‌ی فروشنده | `ارزیابی-فروشنده-پگاه` | **تست‌شده** — قاعده‌ی کسب‌وکاری |
| درجه‌بندی و رتبه‌ی مشتری | `رتبه-بندی-مشتریان-پگاه` | **تست‌شده** — قاعده‌ی کسب‌وکاری |
| واژگان مشترک: افراد، مرکز، تقویم، گروه، بانک | `تسلط-مشترک-پگاه` | نقشه‌ی `Global` |
| فروش، فاکتور، مشتری، هدف، تخفیف، مرجوعی | `تسلط-فروش-پگاه` | نقشه‌ی `Sales` |
| انبار، موجودی، کاردکس، کالا، بچ | `تسلط-انبار-پگاه` | نقشه‌ی `Warehouse` |
| خزانه، دریافت/پرداخت، چک، تنخواه | `تسلط-خزانه-پگاه` | نقشه‌ی `Treasury` |
| پرسنل، حقوق، کارکرد، بیمه، وام | `منابع-انسانی-پگاه` | نقشه‌ی `HumanResource` |
| ساختنِ نقشه برای یک اسکیمای تازه | `ساخت-اسکیل-اسکیما-پگاه` | ابزار |

**دو اسکیلِ تست‌شده مقدم‌اند.** اگر سؤال دقیقاً «نمره‌ی فروشنده» یا
«رتبه‌ی مشتری» است، از آنها شروع کن؛ اسکیل نقشه فقط زمینه می‌دهد.
دوباره نسازشان.

## پنج اسکیمای نقشه‌شده

| اسکیما | اسکیل | جدول | یال داخلی | مقصدِ بیرونی | ccِ بیرونی |
|---|---|---:|---:|---:|---:|
| `Global` | `تسلط-مشترک-پگاه` | ۱۱۹ | ۱۱۸ | ۱۰ | ۵۴ |
| `Sales` | `تسلط-فروش-پگاه` | ۴۳۵ | ۶۰۴ | ۶۳ | ۲۳۸ |
| `Warehouse` | `تسلط-انبار-پگاه` | ۱۷۰ | ۱۹۲ | ۳۷ | ۱۰۹ |
| `Treasury` | `تسلط-خزانه-پگاه` | ۱۵۲ | ۱۱۰ | ۴۳ | ۱۵۶ |
| `HumanResource` | `منابع-انسانی-پگاه` | ۴۱۳ | ۴۳۳ | ۲۸ | ۱۱۱ |

دیتابیس در هر پنج نقشه `PegahAI` است. **دو اسکیلِ تست‌شده اتصالشان را
`pakhsh` می‌نامند** — نامِ اتصال، نه نامِ کاتالوگ. نامِ اسکیما و جدول در هر
دو یکی است؛ اگر کوئری روی نامِ دیتابیس شکست خورد، همین را چک کن.

## چه کسی از چه کسی می‌خواند

سطر = مبدأ، ستون = مقصد. عدد = تعداد جدولِ مقصدِ متمایز.

| از \ به | Global | Sales | Warehouse | Treasury | HumanResource |
|---|---:|---:|---:|---:|---:|
| **Global** | · | ۲ | ۱ | — | ۴ |
| **Sales** | ۳۸ | · | ۱۱ | ۲ | ۴ |
| **Warehouse** | ۱۹ | ۱۰ | · | — | ۲ |
| **Treasury** | ۲۲ | ۷ | ۵ | · | ۴ |
| **HumanResource** | ۲۳ | ۱ | — | ۱ | · |

سه چیز را این جدول می‌گوید:

- **`Global` واژگان است، نه دامنه.** ۱۰۲ ارجاع از چهار اسکیمای دیگر
  به آن، و فقط ۷ ارجاع از آن به بیرون.
  هر ابهامی که آنجا حل نشود در چهار اسکیل دیگر تکرار می‌شود — **اول آنجا.**
- **`Treasury` مقصدِ کمی دارد ولی مبدأِ زیادی.** در این خانواده فقط
  `Sales` و `HumanResource` به آن اشاره می‌کنند، ولی خودش به هر چهارتای
  دیگر سر می‌زند: جایی است که تعهدِ دامنه‌های دیگر به پولِ واقعی تبدیل
  می‌شود.
- **`Warehouse` و `Treasury` مستقیم به هم وصل نیستند** جز یک‌طرفه:
  خزانه ۵ جدولِ انبار را می‌خواند، انبار هیچ.

## سه پلِ اصلی

```text
شخص     HumanResource.Personel.ccAfrad  →  Global.Afrad.ccAfrad
                                        ←  Sales.AmarForosh_Arshive.ccAfradForoshandeh
کالا    Sales.ccKalaCode                →  Warehouse.Kala   (شناسه‌ی دوم، نه ccKala)
پول     HumanResource / Sales / AssetAccounting
                                        →  Treasury.EjazehPardakhtFaktor
```

**پلِ شخص مهم‌ترین است**: پرونده‌ی پرسنلی یک نفر را به عملکرد فروشش وصل
می‌کند. و `ccAfradForoshandeh` جدولی به این نام ندارد — `Afrad` در نقشِ
`Foroshandeh` است.

## چهار چیزی که فقط با کنارِ هم گذاشتنِ نقشه‌ها دیده می‌شود

`crosslink.py` این چهار را می‌سازد و در هر `schema.json` زیر کلیدِ
`cc_resolution` می‌گذارد. **همه‌شان نامزدند، نه کلید خارجی.**

### ۱. ارجاعِ نقش‌دار — ۲۶۴ مورد

`ccAfradForoshandeh`، `ccMarkazAnbar`، `ccAnbarBe`: نامِ یک موجودیت + نقش.
با یک اسکیمای تنها «بی‌مقصد» به نظر می‌رسند؛ با فهرستِ جدولِ همه‌ی
اسکیماها مقصدشان پیداست.

| اسکیما | نقش‌دار | بزرگ‌ترین |
|---|---:|---|
| `Sales` | ۱۰۳ | `ccMarkazForosh` ×۱۲۴ → `Global.Markaz` |
| `Warehouse` | ۵۳ | `ccMarkazAnbar` ×۳۷ → `Global.Markaz` |
| `Treasury` | ۴۸ | `ccMarkazAnbar` ×۱۷ → `Global.Markaz` |
| `HumanResource` | ۳۵ | `ccAfradModir` ×۳ → `Global.Afrad` |
| `Global` | ۲۵ | `ccAfradModir` ×۳ → `Global.Afrad` |

`ccMarkazForosh` با ۱۴۸ ارجاع در کل خانواده بزرگ‌ترینِ اینهاست و **در هیچ
اسکیل قبلاً نوشته نشده بود**. `Global.Markaz` نامزدِ آن است — نه
`Global.MarkazPakhsh`، که چیزِ دیگری است.

### ۲. ارجاعِ دوپهلو — دو مقصدِ هم‌نام

| ارجاع | کجا | نامزدها |
|---|---|---|
| `ccKala` | `Sales` ×۱۵ | `Amargar.Kala` **یا** `Warehouse.Kala` |
| `ccBrand` | `Sales` ×۳۰، `Treasury` ×۳، `Global` ×۱ | `Amargar.Brand` **یا** `Warehouse.Brand` |

سازنده‌ی نقشه اولی را برداشت چون اول در فهرست بود. **این تنها جایی است که
نقشه می‌تواند بی‌صدا غلط باشد** — join کار می‌کند و عددِ اشتباه می‌دهد.
در `Sales` ارجاعِ کالا معمولاً روی `ccKalaCode` است (۶۸ ارجاع، تأییدشده به
`Warehouse.Kala`)؛ آن ۱۵ تا `ccKala` استثنا هستند و باید جدا بررسی شوند.

### ۳. یک شناسه با دو املا — ۱۱ خوشه

`join` روی املای اشتباه **خطا نمی‌دهد؛ صفر سطر می‌دهد.**

| درست | غلط‌املا |
|---|---|
| `ccMarkazPakhsh` (۱۳۴) | `ccMarkazpakhsh` (۱) |
| `ccTaminKonandeh` (۷۲) | `ccTaminkonandeh` (۴) |
| `ccSazmanForosh` (۳۹) | `ccSazmanforosh` (۱) |
| `ccDarkhastFaktor` (۲۴) | `ccdarkhastFaktor` (۱)، `ccDarkhastfaktor` (۱) |
| `ccHesab4` (۴) | `cchesab4` (۱) |

فهرست کامل در `skills-index.json` کلیدِ `spelling_variants`، و از هر اسکیل
با `map.py crossref` (بدون آرگومان).

### ۴. ارجاعِ واقعاً بی‌مقصد

در **هیچ‌کدام** از پنج اسکیمای نقشه‌شده جدولی به این نام نیست:

| ارجاع | جمعِ خانواده | کجا |
|---|---:|---|
| `ccUser` | ۱۳۶ | هر پنج اسکیما |
| `ccUserSabtKonandeh` | ۷۲ | HR، خزانه |
| `ccUserSabegheh` | ۵۳ | چهار اسکیما |
| `ccShahr` | ۲۶ | Global، HR، فروش |
| `ccNoeSenf` | ۲۵ | فروش |
| `ccElat` | ۲۱ | سه اسکیما |

**هویت کاربر (`ccUser*`، ۲۶۱ ارجاع) و جغرافیا (`ccShahr`، `ccOstan`،
`ccShahrestan`، `ccMantagheh`) دو حفره‌ی بزرگ‌اند** و احتمالاً در اسکیمایی
هستند که هنوز نقشه ندارد. **جدولِ محلی برایشان نساز و «وجود ندارد» هم
نگو** — بگو نقشه‌شان را نداریم.

دو تا از این‌ها معنایشان از سند می‌آید، نه از نام — `crosslink.py` آنها را
`documented` علامت می‌زند:

| ارجاع | مقصد | فیلتر |
|---|---|---|
| `ccNoeMoshtary` (۴۸) | `Global.Goroh` | `ccGorohLink = 304` |
| `ccGorohKala` (۲۳) | `Global.Goroh` | `ccGorohLink = 560` |

## کار با نقشه‌ها

هر پنج اسکیلِ نقشه **همان یک اسکریپت** را دارند، پس دستورها همه‌جا یکی
است:

```
table  refs  path  find  domain  external  crossref  traps  drift
```

```python
import subprocess, sys
r = subprocess.run(
    [sys.executable, "skills/تسلط-فروش-پگاه/scripts/map.py",
     "crossref", "ccAfradForoshandeh"],
    capture_output=True, text=True, encoding="utf-8")
print(r.stdout or r.stderr)
```

با `kind='python'` اجرا کن، نه `kind='bash'` — روی میزبان ویندوزی خروجی
فارسی با کدپیج ویندوز به هم می‌ریزد.

`منابع-انسانی-پگاه/scripts/hr_map.py` نامِ قدیمیِ همین است و هنوز کار
می‌کند؛ در نوشته‌های تازه `map.py` را صدا بزن.

## ساختنِ دوباره‌ی این صفحه

```bash
python ساخت-اسکیل-اسکیما-پگاه/scripts/crosslink.py report .   # فقط چاپ
python ساخت-اسکیل-اسکیما-پگاه/scripts/crosslink.py apply  .   # می‌نویسد
```

`apply` بلوکِ `cc_resolution` را در هر `schema.json` تازه می‌کند و
`skills-index.json` را می‌سازد. **بعد از هر بازسازیِ نقشه یک بار بزنش** —
وگرنه ارجاع‌های بین‌اسکیمایی عقب می‌مانند.

## قاعده‌ای که در هر هشت اسکیل یکی است

> **نقشه می‌گوید کجا را نگاه کنی؛ جواب از دیتابیس می‌آید.** نقشه برای پیدا
> کردنِ جدول، مسیر اتصال، دانه و تله قابل اعتماد است. ولی **هیچ عددی در
> گزارش از نقشه نمی‌آید.**

و هیچ یالی در این خانواده کلید خارجیِ اثبات‌شده نیست — `foreign_keys_declared`
در هر پنج نقشه صفر است. `HIGH` یعنی نام دقیقاً می‌خواند، نه اینکه دیتابیس
تأییدش کرده.
